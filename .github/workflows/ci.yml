name: 🕷️ Tenebri CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4

    - name: 🟡 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: 📦 Install Dependencies
      run: npm install

    - name: 🧪 Run Bot Build Check
      run: |
        echo "✅ Bot files present."
        node -v
        npm -v

    - name: 🚀 Run Lint (Optional)
      run: echo "No linting configured yet."
